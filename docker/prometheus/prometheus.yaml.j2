# Prometheus config jinja2 template.
# This compose jinja2 template can be configured by the corresponding Ansible playbook
# or by the script `docker/generate` in case of standalone installation.
#
# .. code:: bash
#
#     $ docker/generate --config docker/docker.yaml docker/prometheus

global:
    scrape_interval: 10s
scrape_configs:
    - job_name: prometheus
      static_configs:
        - targets:
          - {{ prometheus.host }}:{{ prometheus.port }}
    - job_name: exporter
      static_configs:
        - targets:
            - {{ exporter.host }}:{{ exporter.port }}
