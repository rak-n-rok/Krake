---

flavor: M.mem+

minikube_api_port: &minikube_api_port
  9443

secrules:
  # Minikube
  - port: *minikube_api_port
    protocol: tcp

allow_icmp: False

# App
#######


use_keystone: false

prometheus_port: 31000

# Minikube and kubectl configuration parameters
minikube_install_dir: /usr/local/bin
minikube_version: v1.4.0
kubectl_version: v1.16.2

# Keystone configuration parameters
kube_api_config: /etc/kubernetes/manifests/kube-apiserver.yaml
minikube_path: /var/lib/minikube/certs
user_role: _member_
user_project: demo
